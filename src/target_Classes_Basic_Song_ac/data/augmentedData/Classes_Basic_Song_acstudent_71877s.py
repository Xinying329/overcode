import random
random.seed(1)
import pylab
import numpy
class Song(self):
    def __int__(self,title,len):
        self.title=title
        self.len=len
    def __str__(self):
        return self.title+", "+self.len
s=Song('Respect',150)
