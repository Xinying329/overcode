import random
random.seed(1)
import pylab
import numpy
class Song:
    def __init__(self,title,num):
        title=self.title()
        num=self.num()
    def __self__(title,num):
        return title+', '+num
s=Song('Respect',150)
