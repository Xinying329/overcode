import random
random.seed(1)
import pylab
import numpy
class Song:
    def __init__(self,title,len):
        title=self.title
        len=self.title
    def __str__(self):
        return title+", "+len
s=Song('Respect',150)
