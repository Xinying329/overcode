import random
random.seed(1)
import pylab
import numpy
class Song:
    def __init__(self,title,len):
        title=self.title
        len=self.len
    def __str__(self):
        return str.title+", "+str.len
s=Song('Respect',150)
